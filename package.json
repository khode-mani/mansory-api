{
  "name": "mansory-json-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx json-server --watch db.json --port 5000",
    "start": "npx json-server --watch db.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}

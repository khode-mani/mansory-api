{
  "name": "mansory-json-api",
  "version": "1.0.0",
  "description": "Fake REST API with json-server for Mansory project",
  "main": "db.json",
  "scripts": {
    "dev": "json-server --watch db.json --port 5000",
    "start": "node ./node_modules/json-server/lib/cli/bin.js --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "keywords": [
    "json",
    "api",
    "fake",
    "rest"
  ],
  "author": "Mani",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
